{
  "name": "medisync-backend",
  "version": "1.0.0",
  "description": "Node.js Backend API for MediSync Priority Queue System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node seed.js",
    "seed:queue": "node seed_queue_system.js",
    "seed:all": "node seed.js && node seed_queue_system.js",
    "seed:reset": "node -e \"require('./config/mongodb').initMongoDB().then(() => { const mongoose = require('mongoose'); mongoose.connection.db.dropDatabase().then(() => { console.log('Database reset'); process.exit(0); }); })\" && npm run seed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "healthcare",
    "queue-management",
    "appointment-booking",
    "priority-queue"
  ],
  "author": "MediSync Team",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "joi": "^17.11.0",
    "mongoose": "^9.0.0",
    "morgan": "^1.10.0",
    "redis": "^4.6.10",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
